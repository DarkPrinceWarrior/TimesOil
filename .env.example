# Non-secret Compose interpolation; Qwen key stays in this UID 10001-readable file.
# For remote access, keep loopback binding and use an SSH tunnel or reverse proxy.
AIOS_PORT=8000
QWEN_API_KEY_FILE=/dev/shm/timesoil-qwen-api-key
