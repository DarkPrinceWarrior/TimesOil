# Рабочий журнал — TimesOil

Схема записи каждой задачи: **Plan → Act → Verify → Report**.

## Журнал

| Дата | Действие | Результат | Статус |
|------|----------|-----------|--------|
| 2026-07-04 | Инициализация репозитория: каркас тулинга агентов (`CLAUDE.md`/`AGENTS.md` побайтно, `.serena/project.yml` python, `.gitignore`, `README.md`, `docs/roadmap.md`). MCP-инструменты глобальные (serena/tavily/fff/codegraph + плагины context7/playwright/honcho) — наследуются. codegraph обновлён 1.1.3→1.2.0. | Публичный GitHub-репозиторий создан, продублирован на a100 `/root/projects/TimesOil`. Готово к работе нового агента. | ✅ |
| 2026-07-07 | **Этап 5.Д: финализация.** Plan: прогноз вперёд ансамблем, PDF со всеми скважинами, полный документ истории. Act: `forecast_forward.py` переведён на ансамбль (5 компонентов, эмпирические интервалы 80 % по остаткам 14 срезов), `forecast_forward_nf.py` (TiDE в своём окружении), `make_pdf.py` (37 страниц: сводка, карта, веса+важности признаков, 33 скважины), `история_проекта_полная.md`. Verify: прогоны на a100, PDF проверен визуально. | Прогноз вперёд: нефть по полю 280→275 т/сут, жидкость 5787→5832; PDF `отчёт_приложение_скважины.pdf`; отчёт § 4/§ 5 обновлены; полная история проекта в отдельном документе. Этап 5 завершён целиком. | ✅ |
| 2026-07-07 | **Этапы 5.Б–5.Г: сети, 14 срезов, ансамбль.** Plan: Б — сети neuralforecast; В — расширенная проверка; Г — ансамбль без утечки. Act: `run_nf.py` (отдельный venv, GPU 5; BiTCN разошлась — NaN, TSMixerx мультивариатная — заменена TiDE), `run_extended.py` (14 срезов: CRM/наивный/Арпс/Джентил/TiRex-2/Chronos-2), `--ext` у LightGBM/сетей, `run_ensemble.py` (топ-2/обратные ошибки/NNLS). Verify: прогоны на a100; грабля: `uv sync` без `--extra tirex` сносит tirex-2. | **Ансамбль NNLS — новый рекорд: нефть 5.10 % WAPE, жидкость 3.87 %** (канонич. срезы; было 6.40/4.38). Веса: Chronos-2 0.31 + физика 0.27 + TiDE 0.22 + TiRex-2 0.11 + LightGBM 0.10 (нефть). Обзор § 7. | ✅ |
| 2026-07-07 | **Этап 5.2: LightGBM (mlforecast).** Plan: глобальный бустинг с закачкой/CRM/давлением/статикой, 3 полигона. Act: `run_lgbm.py` (лаги 1-12, скользящие, признаки future-known через X_df; исправление: скважины с ранним концом истории исключаются — окна mlforecast строятся от конца каждого ряда). Verify: прогоны на a100. | **Наше поле: жидкость 4.38 % (= CRM, лучший наравне), нефть 7.81 % (= TiRex-2)** — обучаемая линия работает, третий компонент ансамбля готов. UNISIM/Volve: 33/41 % нефть — обучаемым моделям нужна локальная история, переносимы только физика и предобученные. Обзор § 6. | ✅ |
| 2026-07-07 | **Этап 5.1: Chronos-2 на трёх полигонах** (Toto исключён решением). Plan: раннер с табличным интерфейсом (закачка future, давления past, ряд CRM future), варианты u/m/cov/cov_crm. Act: `chronos_runner.py`, `run_chronos.py` (3 полигона одним скриптом), `crm_stack` вынесены в модули unisim/volve. Verify: прогоны на a100. | **Наше поле: Chronos-2 cov_crm нефть 6.73 % WAPE — лучше TiRex-2 (7.83 %), почти догнал CRM×Джентил (6.40 %); Volve: Chronos-2 m — лучший по нефти (29.9 %)**; UNISIM — TiRex-2 впереди; жидкость — за CRM/TiRex-2. Ни одна модель не доминирует → обе в ансамбль. Результаты в `docs/обзор_моделей_и_библиотек_2026-07.md` § 5. | ✅ |
| 2026-07-07 | **Обзор моделей и библиотек (проверка внешнего рейтинга + darts/Nixtla).** Plan: проверить фактуру по Toto-2/Timer-S1/Falcon-X/TimeRouter/MS-FLOW, изучить darts и neuralforecast, найти свежие подходящие модели. Act: 3 параллельных агента (карточки HF, живые таблицы GIFT-Eval/fev-bench, клоны и смоук-тесты darts 0.45.0 / neuralforecast 3.1.9). Verify: цитаты из первоисточников, установка на Python 3.13. | Toto-2 без сопутствующих рядов (не кандидат); главный пропуск обзора — **Chronos-2** (нативные ковариаты, №1 fev-bench); MS-FLOW не существует; darts: Chronos2Model + бэктест-контур + конформные классы; Nixtla: mlforecast+LightGBM, statsforecast-эталоны, лёгкие сети BiTCN/NHITS. План этапа 5 (~3-4 дня). Документ: `docs/обзор_моделей_и_библиотек_2026-07.md`. | ✅ |
| 2026-07-07 | **Этап 4в: наработка часов (Volve) и CRM-P (UNISIM).** Plan: снять «шум простоев» нормировкой на отработанные сутки; проверить компенсацию Pзаб в CRM. Act: `volve.py` (uptime, oil_eff/liq_eff), `run_volve_uptime.py` (две оценки: физическая и календарная при известном плане работ), `crm.py` (CrmCompensated), CRM-P в `run_unisim.py`. Verify: прогоны на a100. | **Volve: наработка сняла ~1/3 ошибки по нефти (0.32→0.24) и ~1/2 по жидкости (0.25→0.13)**; TiRex-2 ≈ наивный по WAPE при вдвое меньшей медианной ошибке и смещении. UNISIM: CRM-P 27.7 % против 26.2 % — компенсация Pзаб не спасает, дело в шуме и коротком окне (отрицательный результат зафиксирован). Отчёт § 7.1. | ✅ |
| 2026-07-07 | **Этап 4б: Volve — третий полигон (реальные промысловые данные).** Plan: parquet-выгрузка petrodb (Equinor, 2008-2016, 5 добывающих + 2 нагнетательные, F-5 переведена из добычи, F-1C остановлена в 2016-04). Act: `volve.py`, `run_volve.py` (CRM по скважине от её старта, месяцы без факта исключены). Verify: прогон на a100. | Реальный промысел самый трудный: WAPE 25-35 % у всех (простои непредсказуемы). Наивный лучший по помесячному WAPE (нефть 32.2 %), но смещение суммы за 6 мес +22 %; **TiRex-2 cov_crm: WAPE 33.5 % при смещении −0.2 %** — для планирования добычи предпочтительнее. CRM в лоб не переносится (простои ломают допущения). Отчёт § 7.1-7.2. | ✅ |
| 2026-07-07 | **Этап 4: переносимость контура — эталон UNISIM-I-H (п. 5 плана).** Plan: скачать историю эталона, обобщить контур, 3 среза × 6 мес. Act: тайди-CSV из CMG-файлов (14+11 скв., 2013-2024, шумные «наблюдённые» ряды), `unisim.py` (блоки по разлому f3: восточный PROD023A/24A/25A+INJ007/010), обобщены `tirex_runner` (группы) и `crm.py` (старт), `run_unisim.py` (9 моделей). Verify: прогон на a100. | Контур переносится за один прогон. Лидер сменился: **TiRex-2 нефть 15.6 %** (наивный 16.7 %, Арпс 31 %), жидкость — все у уровня наивного (14.2-14.9 %); физический стек деградировал (CRM 26 % — переменное забойное давление без компенсации, шум). Вывод: выбор модели зависит от характера данных; контур делает выбор объективным. Отчёт § 7. | ✅ |
| 2026-07-07 | **Этап 3: оптимизация распределения закачки (п. 6 плана).** Plan: множители режима 16 нагнетательных, сумма закачки фиксирована, границы ±20/±30 %; цель — нефть за 6 мес по стеку CRM × Джентил. Act: `optimize_injection.py` (последовательное квадратичное программирование, нормировка цели/ограничения), `crm.py` разделён на подгонку/прогноз. Verify: прогон на a100, сходимость за 10–12 итераций, решение интерпретируемо (вода из промытых блоков B/B2/C в блок E и A). | **+1 384 т нефти (+2.75 %) при ±20 %; +2 059 т (+4.09 %) при ±30 %** без роста суммарной закачки. Отчёт § 6, `results/injection_optimization*.csv`, `figures/injection_opt.png`. | ✅ |
| 2026-07-07 | **Этап 2: физическая обвязка прогноза (пп. 1–4 плана).** Plan: CRM → адресная закачка → калибровка интервалов → обводнённость; прогоны на a100. Act: `crm.py`+`run_crm.py` (pywaterflood, по блокам + полевой без ограничений), `allocation.py` (гидропроводность/связность), `calibrate_intervals.py` (14 срезов, множители без утечки), `fractional.py`+`run_fractional.py` (Джентил), `forecast_forward.py` переведён на стек. Verify: все прогоны на a100, сводка `collect_results.py`. | **Жидкость: CRM WAPE 4.4 %** (было 7.7 %); **нефть: CRM × Джентил 6.4 %** (было 7.8 %). Блоки подтверждены данными: 66.7 % связности полевого CRM внутри блоков (доля пар 21.4 %). Накрытие интервалов жидкости 0.59→0.73. Прогноз вперёд обновлён (нефть 282→268 т/сут). Отчёт дополнен (§ 3.5, § 4, § 5). | ✅ |
| 2026-07-07 | **Обзор литературы и разведка направлений развития.** Plan: изучить 12 статей из `papers/` (3 параллельных читателя) + веб-разведка tavily по 6 направлениям. Act: разобраны кластеры «физика/связность», «машинное обучение», «смешанный»; найдены библиотека pywaterflood (ёмкостно-резистивные модели), методы конформной калибровки интервалов (AcMCP), фракционные модели обводнённости (Ковал/Джентил), открытый эталон UNISIM-I-M. Verify: три независимых источника (статьи, веб, собственный анализ) сошлись на одном векторе. | Утверждён план этапа 2 (см. раздел «Этап 2 — план»); направления развития зафиксированы в отчёте (§ 6). Вывод: выигрыш — в физической обвязке лидера (связность, ёмкостно-резистивные модели, калибровка), а не в новых обучаемых архитектурах. | ✅ |
| 2026-07-04 | **Задача 1: прогноз дебита на 6 мес (TiRex-2 + SPDM).** План: изучить `raw_data` (2 Excel + карта разломов) → исследовать модели → постановка (3 среза × 6 мес, WAPE) → пайплайн (`src/timesoil`, `scripts/`) → прогоны (TiRex-2 локально на CPU, SPDM на a100 GPU 5, tmux) → отчёт. Act: оцифрованы 5 разломов с карты → 6 блоков; выявлены ловушки данных (THP=пластовое, DobG=жидкость, мусорный 2015-12, нули до пуска, перевод 15 скв. под нагнетание в 2008). SPDM: 3 дефекта репо исправлены (конфиги, freq `ME`, путь чекпоинта), 8 обучений на a100. Verify: сверка двух Excel (тождественны), бэктест 10 моделей/конфигураций. | **TiRex-2 blocks_cov лучшая: нефть WAPE 8.3 %, жидкость 7.7 %** (Арпс 9.4 %/10.3 %, SPDM 10.0 %/12.5 %). Закачка по блокам как известный план — главный фактор (жидкость 13.4→7.7 %). Прогноз вперёд 2015-12..2016-05 в `results/forward_*.csv`. Отчёт: `docs/отчёт_прогноз_дебита_6мес.md`. | ✅ |

## Этап 2 — план (по итогам обзора литературы, 2026-07-07)

Приоритет по соотношению «эффект/затраты»; статусы обновляются по мере выполнения.

| № | Шаг | Суть | Статус |
|---|-----|------|--------|
| 1 | Ёмкостно-резистивная модель (pywaterflood) | CRMP по блокам: матрица связности $f_{ij}$ (16×33) — проверка блоков данными; физичный эталон жидкости в скользящей проверке; прогноз CRM на 6 мес как известная наперёд сопутствующая величина для TiRex-2 (рецепт Nagao & Datta-Gupta, 2026) | ✅ 4.4 % WAPE жидкость; блоки подтверждены (66.7 % связности внутри) |
| 2 | Адресная закачка вместо блочных сумм | Веса гидропроводности $w_{ij}=\bar{k}_{ij}/d_{ij}^{\eta}$ из статики (STA-MGCN, 2025) и/или веса связности $f_{ij}$ из шага 1; сопутствующая величина на каждую скважину | ✅ нефть 7.9 % (лучше блочных сумм), но ряд CRM сильнее |
| 3 | Конформная калибровка интервалов | Расширить скользящую проверку до ~10 срезов; погоризонтные поправочные множители квантилей по остаткам (AcMCP, Ванг и Хиндман); цель — накрытие 80 % вместо 70/52 % | ✅ жидкость 0.59→0.73; нефть без изменений (0.74) |
| 4 | Физичное разделение нефть/жидкость | Фракционная модель обводнённости (Ковал — молодые, Джентил $f_o=1/(1+\alpha W^{\beta})$ — зрелые); нефть = жидкость × (1 − обводнённость); устраняет рассогласование раздельных прогнозов | ✅ нефть 6.4 % WAPE (CRM × Джентил) |
| 5 | Второе месторождение | Перенос контура на открытый эталон UNISIM-I (14 добывающих + 11 нагнетательных) | ✅ UNISIM-I-H: TiRex-2 лучшая (нефть 15.6 %), физический стек чувствителен к шуму и переменному Pзаб |
| 7 | Volve из petrodb — третий полигон (реальные данные) | прогон тем же контуром | ✅ наивный 32 % WAPE, TiRex-2 cov_crm — смещение −0.2 % против +22 % у наивного |
| 6 | Оптимизация распределения закачки | Сценарии «перекинуть закачку между блоками» поверх прогнозной модели; максимум нефти при фиксированной суммарной закачке | ✅ +2.75 % (±20 %) / +4.09 % (±30 %) нефти |

## Онбординг

- **Назначение проекта:** прогноз дебита нефти/жидкости скважин на 6 месяцев
  (месторождение с разломами, поздняя стадия заводнения, данные симулятора).
- **Технологический стек:** Python 3.13 + uv; pandas/numpy/scipy/matplotlib;
  TiRex-2 (zero-shot, extra `tirex`, работает на CPU); SPDM/ManiMamba
  (обучение на a100, отдельное окружение `external/spdm/.venv`, py3.12+mamba-ssm).
- **Структура и точки входа:** `src/timesoil/` (data/wells/metrics/baselines/
  backtest/tirex_runner + этап 2: crm/allocation/fractional) + `scripts/`
  (`run_baselines.py`, `run_tirex.py`, `run_crm.py`, `run_fractional.py`,
  `calibrate_intervals.py`, `optimize_injection.py`, `prepare_spdm_data.py` → `spdm_run.sh` →
  `eval_spdm.py`, `forecast_forward.py`, `collect_results.py`, `make_figs.py`).
  Данные `raw_data/` вне git (передавать `scp`), результаты `results/` вне git.
