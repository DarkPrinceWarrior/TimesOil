# Роли — одна матрица на оба трека

Дата: 2026-08-17.
Источники: `architecture_2026-08-17.md`, `t1-layout.md`, `t2-layout.md`, `design.md`.
Это **спека**, не SVG. Имена не переименовывать.

Одна таблица на трек 1 и трек 2. Суррогат — только трек 2. TimesOil ≠ `SurrogateWorldModel`.

Формулы колофона:

- трек 1: `LLM = ДЕЙСТВИЯ · CRM = ОТСЕВ · ГДМ = ФИЗИКА МЕСЯЦА · CHDD_PYTHON = ДЕНЬГИ`
- трек 2: `СУРРОГАТ = ПОИСК · OPM = ИСТИНА ПЛАНА · CHDD = ДЕНЬГИ`

Токены — из `design.md`. `--gold` не для текста.

| роль | делает | не делает | цвет токена |
|---|---|---|---|
| LLM / агенты | Цикл ТЗ: оценка → решение → воздействие → повтор. Пишет `ControlIntent`, выбирает стратегию, вызывает разрешённые инструменты, объясняет из численных фактов. Трек 1: мир = полная ГДМ. Трек 2: мир = суррогат. LLM в ядре обязательна. | Дебиты, давления, ЧДД. `wells_schedule.inc` и deck. Пределы безопасности. Shell. Обход гейта. Финал без ГДМ и `CHDD_PYTHON`. Состояние фонда. | `--ink` `#1C1914` |
| CRM / отсев | TimesOil CRM / CRM2P / CRMP / Джентил: предварительный отсев слабых кандидатов (трек 1). На треке 2 — физический baseline нового суррогата, не сам суррогат. | Restart и каноническое состояние. Физическая верификация плана. Официальный ЧДД. Замена ГДМ, OPM или `SurrogateWorldModel`. | `--muted` `#5A544A` |
| Компилятор графика | Eclipse / tNavigator / OPM include из принятого плана. Сдача — `wells_schedule.inc`. Parser round-trip обязателен. Трек 1: в цикле фрагмент месяца, к горизонту полный файл. Трек 2: `INCLUDE` только в overlay. | CSV как файл сдачи (CSV — вход `CHDD_PYTHON`). Ручная правка после сборки. Дебиты и ЧДД. Запись из свободного текста LLM. | `--gold` `#C4A35A` |
| ГДМ / OPM | Физика принятого управления. Трек 1: полный прогон каждый принятый месяц; состояние из restart. Трек 2: свои прогоны OPM Flow для обучения; один контрольный replay `Model_Z_final_OPM` физически проверяет план; 14 полей из overlay SUMMARY. | Прогнозатор. Деньги. Отсев CRM. Признание плана по одному лишь суррогату. Обновление состояния из LLM или TimesOil. | `--gold-wash` `#F4EBD4` |
| Суррогат (только трек 2) | Дешёвый поиск многих полных графиков. `SurrogateWorldModel`: гибрид CRM / матбаланс + обучаемый остаток; UQ / OOD; WAPE нефти и жидкости. На rollout можно считать `CHDD_PYTHON`. Один лучший график уходит в шлюз. | Физическая верификация. Таблица 14 полей заявки. TimesOil как этот суррогат. Помесячный цикл трека 1. Финальный ЧДД без replay OPM. | `--gold-wash` `#F4EBD4` |
| `CHDD_PYTHON` | Единственный калькулятор. 14 полей; нефть и жидкость — тонны, закачка — м³. Одно число: заявленный ЧДД, млн руб. Трек 1: `--start-year 2014`. Трек 2: `--start-year 2007`. | Другие калькуляторы. Неявный start-year. Контроль 4418,13 и старт 1991. Таблица из суррогата или CRM. | `--gold-ink` `#6B5012` |
| Организаторы | Сертифицируют результат после сдачи. Трек 1: этап 1 — автовалидация тестового кейса, рейтинг по заявленному ЧДД; этап 2 — один прогон сданного `wells_schedule.inc` через ГДМ и тот же `CHDD_PYTHON`. Трек 2: один прогон ГДМ и тот же `CHDD_PYTHON`. Расхождение с заявкой — дисквалификация. | Повторные прогоны. Дампы Model Z. Приём без полного пакета. Правка сданного графика. | `--rust` `#8B3228` |
| UI | Простой экран запуска и разбора. Слой объяснений. Статус МАС / ГДМ / `CHDD_PYTHON`. Выгрузка после зелёного parser round-trip, ГДМ и `CHDD_PYTHON`. В пакете: код, инструкция, конфиг. Docker — только если контейнеризуете. | Правка `wells_schedule.inc`. Численная истина. Замена МАС, ГДМ или `CHDD_PYTHON`. Attention map как доказательство. | `--card` `#FFFCF6` |
