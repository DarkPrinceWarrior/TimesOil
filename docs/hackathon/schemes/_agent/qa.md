# QA — схемы треков (overflow / читаемость)

Дата: 2026-08-17. Файлы сняты с диска после перехода постеров на 2400×1350. SVG не правились.

Метод: геометрия `viewBox` и абсолютные `x/y/width/height`; ширина текста — DejaVu Sans/Serif (стек SVG: Segoe UI / Georgia; запас ±5 %). Контраст — WCAG 2.x relative luminance. HTML `track1.html` / `track2.html` проверены в браузере при 2400×1350: `scrollWidth/Height = clientWidth/Height`, вертикального скролла нет.

Целевое безопасное поле (бриф): **48 px**, коннекторы только внутри **48…2352 × 48…1302**.

---

## Must-fix

- Сетка обоих постеров сидит на **x=36 / right=2364**, не на 48…2352. Карточки, лента, баннеры, пакеты заходят в поле на 12 px слева и справа.
- `track2.svg` чип `топ 20 % ≤ 10` в боксе **(1992, 792, 96×22)** — текст шире поля (+2 px DejaVu, Segoe ещё шире).
- Контраст мелкого золота: `#8f6f2e` на `#efe8d8` = **3.84:1** (кикеры 12 px); `#8f6f2e` на `#1b241c` = **3.41:1** (теги пакета 10 px); колофон `#8a8068` на `#efe8d8` = **3.20:1**. AA normal 4.5 не проходит.
- `render/*.svg` — старые mermaid-листы: высота до **4543**, кластеры на **x=8 / y=8**, стрелки `#333` на `#1a1c19` (1.36:1). В 2400×1350 не помещаются.

---

## Safe-area 2400×1350

| | px |
|---|---:|
| Холст | 2400 × 1350 |
| Поле | 48 |
| Контент / коннекторы | **48…2352 × 48…1302** (2304 × 1254) |
| Колонки (как в `t1-layout.md`) | 4 × 558, gutter 24 → x = **48, 630, 1212, 1794** (правый край 2352) |
| Рамка, если нужна | внешняя (48, 48, 2304, 1254); внутренняя (54, 54, 2292, 1242) — не (20, 20) |

Сейчас нарисовано: рамка **(20, 20, 2360, 1310)** и **(26, 26, 2348, 1298)**; карточки **570** px с x = **36, 622, 1208, 1794**. Сдвиг: −12 px слева, +12 px ширины, правый край **2364**.

Коннекторы ленты (шевроны `M317…` … `M2063…`, y=176…188) уже внутри 48…2352 × 48…1302. Двигать нужно сетку карточек и рамку, не стрелки.

Старый fault `x1=1288 y1=-40 x2=… y2=2240` в текущих `track1.svg` / `track2.svg` **снят**.

---

## `docs/hackathon/schemes/track1.svg`

`viewBox 0 0 2400 1350`. Fault за пределы холста нет. `marker-end` нет. Шевроны ленты не на кромке viewBox.

### 1. Выход из safe-area / обводка

| Элемент | Координаты | Дефект |
|---|---|---|
| Внешняя рамка | (20, 20, 2360×1310) → 20…2380 × 20…1330 | L/T/R/B вне 48…2352 × 48…1302 |
| Внутренняя рамка | (26, 26, 2348×1298) → 26…2374 × 26…1324 | то же |
| Лента 01–08 | (36, 156, 2328×52) → 36…2364 × 156…208 | L=36, R=2364 |
| Карточки 01, 05 | x=36, w=570 | L=36 |
| Карточки 04, 08 | x=1794, w=570 → R=2364 | R=2364 |
| Баннер возврата | (36, 876, 2328×40) → 36…2364 | L/R |
| Пакет 03 | (1604, 966, 760×250) → R=2364 | R=2364 |
| Колофон | y=1288, x=48…2352 | внутри поля по y (до 1302) |

Обводка рамок `stroke-width=1.25` остаётся внутри 0…2400. За viewBox ничего не выходит.

### 2. Текст / чипы

Чипы 11 px в целом проходят (запас ≥12 px). Тела карточек 13 px в 570 px — не клипаются.

Запас меньше 16 px (риск на Segoe/Arial):

- `WELSPECS × 49` (1224, 502, 118×22) — запас ~13 px
- `WCONPROD` (1350, 502, 96×22) и (294, 834, 96×22) — ~13 px
- `заявленный ЧДД` (1976, 834, 128×22) — ~14 px

KPI `проверка организаторов` (2196, 88): ширина ≈145, конец ≈2341 < 2352.

### 3. Вертикальный скролл

Сам лист 16:9. Обёртка `track1.html` при 2400×1350 не скроллит. Сырой SVG без `max-width:100%` на окне <2400×1350 даёт скролл браузера.

### 4. Стрелки на кромке

Шевроны ленты: (317, 176)–(325, 182), далее 608, 899, 1190, 1481, 1772, 2063. Все внутри поля. На правом краю ленты стрелки нет.

### 5. Контраст

| Пара | Ratio | Где | Вердикт |
|---|---:|---|---|
| `#1b241c` / `#efe8d8` | 13.08 | заголовок, KPI | AA pass |
| `#3f3b32` / `#fffaf0` | 10.72 | body карточек | AA pass |
| `#5a5648` / `#efe8d8` | 6.02 | lede, note | AA pass |
| `#8f6f2e` / `#efe8d8` | **3.84** | кикер (48, 58), 12 px | AA normal fail |
| `#8f6f2e` / `#1b241c` | **3.41** | теги пакета (54, 990), (838, 990), (1622, 990) | AA normal fail |
| `#8a8068` / `#efe8d8` | **3.20** | колофон y=1288 | AA normal fail |
| `#c9b896` / `#efe8d8` | 1.60 | рамка, штрих карточек | декорация, почти не видна |

### 6. Дубли, мешающие скану

- Лента 01–08 и сетка карточек 01–08 повторяют одни номера и короткие ярлыки (`Приём` / `Приём Model Y`).
- Пакет «01 · ГРАФИК» снова говорит `wells_schedule.inc`, уже стоящий в карточке 05.

---

## `docs/hackathon/schemes/track2.svg`

Тот же холст и та же сетка 36/570. Fault за холст нет.

### 1. Safe-area

| Элемент | Координаты | Дефект |
|---|---|---|
| Рамки | как у трека 1 | 20/26 вне поля |
| Лента | (36, 156, 2328×52) → 36…2364 | L/R |
| Карточки 01–04 | y=248, h=248, x=36…2364 | L/R |
| Мост | (36, 508, 2328×36) → 36…2364 | L/R |
| Карточки 05–08 | y=580, h=248, x=36…2364 | L/R |
| Пакет 04 | (1794, 886, 570×200) → R=2364 | R |
| «НЕ ПУТАТЬ» | (36, 1102, 2328×72) → 36…2364 | L/R |
| 14 полей | первый чип x=36, последний (2168, 1214, 158×28) → R=2326 | L=36; правый край в поле |

Кикер `ПЛАСТ 01` (48, 236) в зазоре лента/карточки (лента низ 208, карточки верх 248) — не клип.

### 2. Чипы

**OVERFLOW:** `топ 20 % ≤ 10` — rect **(1992, 792, 96×22)**, текст (2000, 807). DejaVu 11 px ≈ 82 px, доступно 80 px.

**TIGHT:** `WLPR ≤ 500` — **(930, 792, 88×22)**, запас ≈ 3.5 px.

Остальные чипы (в т.ч. `Model_Z_final_OPM` 168×22, `overlay SUMMARY` 128×22, `черновик ЧДД` 118×22) проходят.

14 полей по 158×28 с якорем middle — не клипаются.

### 3. Скролл / стрелки

Как у трека 1: HTML не скроллит при 2400×1350; шевроны ленты те же x, внутри поля.

### 4. Контраст

Те же пары, плюс:

- кикеры `ПЛАСТ 01` (48, 236), `ПЛАСТ 02` (48, 568), `14 ПОЛЕЙ…` (48, 1204) — `#8f6f2e` / `#efe8d8` 3.84
- `НЕ ПУТАТЬ` (52, 1124) `#8a3324` / `#f6ebe4` ≈ 6.95 — pass
- теги пакетов (54, 910) … (1812, 910) — золото на `#1b241c` 3.41

### 5. Фонд и дубли чтения

- KPI фонда корректен: **103 уникальные скважины**; 92 когда-либо добывающих,
  41 когда-либо нагнетательная, 30 входят в обе группы, поэтому
  `92 + 41 - 30 = 103`.
- Три системы нумерации: лента 01–08, `ПЛАСТ 01/02`, карточки 01–08.
- Мост (1200, 531) повторяет смысл карточки 04 («один лучший уходит»).

---

## `docs/hackathon/schemes/track1.html`

Обёртка: `body.stage` + `img.sheet` (`width: min(100vw, 100vh×16/9)`, `max-height: 100vh`).

При 2400×1350: картинка (0, 0, 2400×1350), скролла нет. На более узком 16:9 лист масштабируется, вертикальный скролл схемы не нужен.

Сырой `track1.svg` (атрибуты `width="2400" height="1350"`, без `max-width`) на меньшем окне скроллится — это не HTML.

Контраст обложки не относится. `theme.css` `.kicker` / `.go` = `#8f6f2e` на `#efe8d8` (3.84) — см. `index.html`.

---

## `docs/hackathon/schemes/track2.html`

Та же обёртка. При 2400×1350 скролла нет.

---

## `docs/hackathon/schemes/index.html`

Не постер. На коротком окне блок `.wrap` (`margin: 8vh auto`) даёт обычный скролл страницы — это не схема.

- `.kicker` / `.go`: `#8f6f2e` на `#efe8d8`, 3.84:1, 0.78 rem — AA normal fail.
- Ссылки `render/track1.png`, `render/track2.png` на диске есть. `render/*-flow.svg` с обложки не торчат, но файлы рядом — другой визуальный язык.

---

## `docs/hackathon/schemes/render/*.svg`

Mermaid, тёмный фон `#1a1c19`. В 48…2352 × 48…1302 **не влезают**. Стрелки дефолт `.flowchart-link { stroke:#333 }` на том же фоне = **1.36:1**.

### `track1-flow.svg`

- viewBox **0 0 1197.63 4542.61**. Масштаб в safe-height: 1254/4543 ≈ 0.28 → ≈330×1254, подписи нечитаемы.
- Кластер `1. Вход` (600.3, **8**, 330×358) — y=8.
- Кластер `3. Агенты` (**8**, 1025.9, 1060.4×360) — x=8; узел `agen_blk` центр (173, 1193.9), левый край ≈43.
- Кластер `5. Мир` (239.6, 1845.9, 950.1×1338.7) → R≈1189.6, до края viewBox ≈8 px.
- Кластер `8. Сдача` низ ≈4534.6 при высоте 4542.6 — 8 px снизу.
- Петля `L_mir_loop_sost_diag_0` идёт по x≈1088 почти на всю высоту.
- Узлы с `max-width:200px` + `break-spaces` (не клип, а принудительный перенос русских фраз).

### `track2-flow.svg`

- viewBox **0 0 662.70 2375**. В 1254 px высоты масштаб ≈0.53 → ≈350×1254.
- 9 кластеров с `style=""` — заливка mermaid `#ffffde` на тёмном холсте, другой язык, чем постеры.
- `1. Вход` y=8; `9. Сдача` (8, 2135, 330×232) → x=8, низ 2367, до 2375 = 8 px.
- Кластеры 6–7 правый край 654.7 при ширине 662.7 — 8 px.
- Ребро `L_chdd_forbid_0` без `marker-end`.
- Стрелки `#333` на `#1a1c19`.

### `track2-search.svg`

- viewBox **0 0 343.73 1002**. Узкий столбец; классы charcoal/amber **нет** — дефолт mermaid `#ECECFF` / `#9370DB`.
- Кластер «Быстрый цикл» (**8**, **8**, 314.3×416).
- Кластер «Медленный…» (43.9, 474, 291.8×520) → R=335.7, B=994 (8 px до края).
- Петля D→A едет по правому борту быстрого кластера.

### `track1-month.svg`

- viewBox **0 0 2818.01 285.5**. Ширина **> 2400** и **> 2352**.
- `restart_t` bbox ≈ (64, **8**)–(182.7, 62) — y=8.
- `nxt` / `restart_t+1` ≈ (2673, 165)–(2810, 219) — 8 px до правого края.
- Стрелки `#333` на `#1a1c19`. Горизонтальная лента на 16:9 даёт пустую высоту или горизонтальный скролл.

PNG `render/track1.png` / `track2.png` — растры новых постеров; дефекты те же, что у SVG.

---

## Что уже закрыто (старый бриф)

- Fault `x1=1288 y1=-40 x2=1088|980 y2=2240` в `track1.svg` / `track2.svg` больше нет.
- Холст постеров 16:9, не 1600×2200.
- HTML-схемы не требуют вертикального скролла на 2400×1350.
- Чипы русских ярлыков в постерах в целом не клипаются (кроме `топ 20 % ≤ 10`).
