# Track 1 — source-bound доказательство v2.1

Каталог содержит побайтовые копии выбранного управляющего include-файла и
итоговых метаданных запуска Model Y на A100. Две последовательности были
проверены полным replay в OPM Flow 2026.04; выбран результат
`opm-full-replay-41bda657ce77290fbd1de404` с ЧДД
`110,78238336080598` млн руб.

Доказанный горизонт — только `2014-01-01/2014-02-01`. Это одномесячное
сквозное доказательство `controls → OPM Flow → ЧДД`, а не полугодовая или
финальная конкурсная сертификация. `organizer_certified=false`, глобальная
оптимальность и улучшение на полном горизонте не заявляются.

Состав:

- `wells_schedule.inc` — выбранный controls-only include;
- `result.json`, `proof.json`, `manifest.json`, `manifest.sha256` — точные
  копии терминальных артефактов A100;
- `submission.json` — локальный паспорт поставки с SHA-256, provenance и
  честными границами доказательства.

Терминальная квитанция:
`../../evidence/model_y_track1_source_bound_a100_v2_1.json`, SHA-256
`b3aebefce30a0a948a84702d3b6fd7598aa183c735f296d9cb7ae5a0003cf050`.
