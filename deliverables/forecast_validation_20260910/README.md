# Проверка прогноза — 10 сентября 2026

Все метрики рассчитаны на A100. Финансовые результаты и графики находятся в [проверенном пакете](../verified_20260910/README.md). Здесь — качество прогноза; оно не следует из прироста ЧДД.

**Разделение треков, 10 сентября:** TimesFM используется только в треке 2.
Все приведённые здесь опыты прогноза Y — история исследований; суррогат
удалён из рабочего трека 1. Новый полный цикл Y завершён через Qwen, OPM и
официальный ЧДД: **+54,885597%**, 49 скважин × 23 месяца, 1127 команд,
23 принятых заключения, 364 проверенных файла; `surrogate_used=false`.
[Полный аудит Y](y-physical-only-full-audit.json),
[протокол Y](y-physical-only-protocol.json),
[остановка прежнего опыта](y-integrated-experiment-cancelled.json).
Обучение Z, независимая оценка на восьми новых физических сценариях
и новый агентский цикл завершены. Метрики ниже; точность остаётся открытой.

## Независимые сценарии

### Новый прямой прогноз Z с ранним кодированием скважин

Обучение завершено: 40 эпох, выбрана эпоха 39 по прежней валидации,
ошибка 0,06818 → 0,04263. Обучены все 330,84 млн параметров; идентичность
скважины и геология поступают перед первым и последним блоками внимания.
Нулевая историческая дисперсия целевых рядов обрабатывается масштабом,
вычисленным только по обучающим данным. [Отчёт обучения](z-early-identity-training.json).

Восемь новых физических сценариев меняют одновременно задания дебита и BHP.
Индексы 0/1/3/4/6 используются для калибровки, 2/5/7 — для независимой проверки.
Оба checkpoint закреплены [до тестового прогноза](z-early-identity-frozen-models.json).
Прогноз прямой, 224 месяца для всех 103 скважин: без будущих наблюдений кандидата
и без опорной физической траектории будущего. Диапазоны по трём тестам:

| Модель | Нефть WAPE, % | Жидкость WAPE, % | Давление RMSE, бар |
|---|---:|---:|---:|
| Предыдущие полные веса, 20 + 60 эпох | 19,817–24,069 | 23,274–32,917 | 66,747–80,608 |
| Раннее кодирование, новая эпоха 39 | **13,014–17,075** | **11,484–20,838** | **19,526–23,748** |

Каждая из трёх метрик улучшилась на каждом тестовом сценарии.
[Новая модель](z-early-identity-independent.json),
[предыдущие веса на тех же сценариях](z-full80-cold-start-independent.json).
Это улучшение ещё не означает достаточной точности: в первом месяце двух
резких изменений BHP ошибка нефти — **64,97% и 71,50%**. Интервалы давления
очень широкие (радиус около 220 бар), но целую траекторию покрывают лишь
в двух из трёх тестов. Автономный выбор по суррогату не сертифицирован.

С выбранными до теста весами выполнен отдельный новый цикл Qwen → OPM → ЧДД
на всех 224 месяцах. Гипотеза изменила покважинные дебиты; ЧДД
**14 092,342999 млн руб., +18,686148%** к базе 11 873,620637 млн руб.
Она уступила сохранённым 14 144,272847 млн руб. (**+19,123503%**).
Физический расчёт, полный официальный калькулятор и критик прошли;
победитель выбран по физическому ЧДД. [Протокол](z-early-identity-policy-protocol.json).

### Предыдущие исследования

Во всех таблицах диапазон по трём отложенным сценариям, индексы 2/5/7. Индексы 0/1/3/4/6 служат отдельной калибровке интервалов. Новые тестовые траектории не участвовали в обучении; веса и правила оценки закреплены до чтения результатов. После изучения этих тестов новое обучение требует нового независимого набора.

| Модель и режим | Нефть WAPE, % | Жидкость WAPE, % | Давление RMSE, бар |
|---|---:|---:|---:|
| Y, новое обучение перехода, свежий набор v3 | **1,360–1,912** | **1,617–1,768** | **2,218–3,074** |
| Y, исходный Google, тот же свежий набор v3 | 5,106–8,710 | 4,142–6,507 | 3,608–6,571 |
| Y, прежнее месячное обучение, набор v2 | 2,888–5,268 | 2,962–4,849 | 2,883–9,410 |
| Y, исходный Google, такое же обновление | 5,796–9,194 | 4,551–6,513 | 4,020–6,278 |
| Z, прямой прогноз 224 месяцев, ранее обученный последний слой | 38,213–39,301 | 31,361–33,150 | 76,159–79,691 |
| Z, прямой прогноз, дообучены все слои: 20 + 60 эпох | 17,746–18,363 | 19,665–20,031 | 70,773–74,751 |
| Z, все слои + физическая опорная траектория | 5,198–6,786 | 6,631–8,744 | 5,637–10,599 |
| Z, последний слой + опорная траектория + локальная поправка | 2,985–6,055 | 1,307–3,255 | 2,538–6,447 |
| Z, абляция: опорная траектория + локальный отклик **без Google** | **0,208–2,051** | **0,100–0,871** | **0,303–1,727** |

Y: 49 скважин × 23 месяца; одновременно изменены дебиты и BHP. Обновление использует только уже наступившие фактические месяцы. Средняя ошибка скрывает переход: в первом месяце обученный прогноз нефти ошибается на **22,48–45,58%**, исходный Google — на 41,74–58,54%. Давление после обучения улучшилось не во всех сценариях. [Полные метрики Y](y-monthly-independent-v2.json).

Z: 103 скважины × 224 месяца. Здесь независимые изменения только BHP в локальной области: дебиты, роли и статусы совпадают с опорным физическим сценарием; для открытых добывающих BHP увеличивается на `15*x` бар, для нагнетательных умножается на `1-0.1*y`; `0≤x≤1`, `0≤y≤1-x/2`. Выход за область отклоняется с требованием полного OPM. Результаты нельзя переносить на изменения дебита, открытие/закрытие или конверсию скважин.

Опорная траектория — ранее рассчитанное физическое будущее другого, заранее известного сценария. Это доступная плановая информация, явно добавленная к прогнозу; будущее проверяемого кандидата не подаётся. Локальная поправка обучена только на пяти старых калибровочных сценариях. Степень выбрана по исключению одного старого сценария; новые тесты не использованы. [Поправка](z-local-correction-training.json), [независимая проверка](z-local-corrected-independent.json), [абляция без Google](z-without-google-ablation.json).

Абляция лучше варианта с Google по всем трём целям и всем трём тестовым сценариям. Приписывать улучшение локальной поправки TimesFM нельзя. Полное дообучение Google действительно меняет все слои и уменьшает ошибку прямого прогноза, но давление остаётся неточным. [Обучение 20 эпох](z-full20-training.json), [продолжение 60 эпох](z-full80-training.json), [его независимые метрики](z-full80-independent.json).

## Новые обучение и интеграция

Дополнительное обучение Y завершено: 16 эпох, 252 обучающих и 56 валидационных окон с шестикратным весом первого месяца; использованы только разрешённые старые калибровочные сценарии. Лучшая эпоха 16, валидационная ошибка 0,02794 → 0,01983. [Отчёт](y-intervention-training.json), [зафиксированный протокол](y-intervention-protocol.json). Свежий физический набор v3 завершён; [независимая оценка](y-intervention-independent-v3.json) дала на всех трёх тестах улучшение нефти, жидкости и давления относительно исходного Google. Первый месяц нефти: **6,50–13,86%**, исходный Google: **40,63–53,16%**. Это отдельный набор от предыдущей таблицы v2; сравнение моделей проводится на одинаковых новых сценариях. При объединении каталогов исправлены только относительные ссылки манифестов на исходные OPM-прогоны: физические данные, веса и разбиение не менялись; происхождение заново полностью проверено.

В Треке 1 реализована отдельная загрузка этих месячных весов с проверкой SHA-256, соответствующего отчёта обучения, геологии и горизонта. Исторические интервалы пересчитываются именно для загруженного месячного прогноза. Реальный [прогноз интеграции](y-monthly-integration-preview.json) на A100 выполнен; проверка восстановления статического последнего слоя и 9 тестов CLI прошли. Это проверка загрузки и прогноза, не новый завершённый 23-месячный управленческий цикл. Ранее подтверждённый +54,915631% остаётся результатом прежнего полного цикла. Новый полный контрольный цикл с обученным месячным монитором запущен отдельно, ещё не завершён: [протокол](y-monthly-controller-protocol.json).

Для Z завершены 40 эпох обучения разности отклика относительно физического эталона. На тех же трёх новых локальных сценариях: нефть 6,671–8,064%, жидкость 8,629–10,278%, давление 7,810–11,609 бар. Этот вариант не превзошёл простую физическую абляцию и не заменяет лучший проверенный вариант. [Обучение](z-reference-response-training.json), [независимый тест](z-reference-response-independent.json).

В Z выявлена причина основной ошибки прямого прогноза: обратная RevIN в закреплённом декодере Google умножает выход на нулевое стандартное отклонение постоянной истории. Прогноз и градиент остаются нулевыми даже при последующем открытии скважины. На старом калибровочном сценарии 0 нулевые истории объясняют **95,224% квадратичной ошибки нефти, 98,181% жидкости и 99,300% давления**; это доли суммы квадратов ошибок, не WAPE. Для давления затронуты 23 скважины, позднее получающие ненулевые значения. [Проверка исходного нормализатора, градиентов и данных](z-cold-start-diagnosis.json).

В коммите `6010dec` добавлен отдельный режим обучения: только для постоянных целевых рядов выходной масштаб берётся из обучающих статистик нефти, жидкости и давления. Геологическое и управляющее обусловливание сохраняются; ковариатные масштабы не меняются. Масштабы записываются в checkpoint и автоматически восстанавливаются загрузчиком. Проверки восстановления весов, градиента и сохранения остальных каналов прошли на A100. Все 20 эпох завершены, выбрана эпоха 18 по старой валидации: ошибка 0,21356 → 0,06818. На старых сценариях разработки прямой прогноз нефти улучшился с 18,52–18,62% до **13,40–14,20%**, жидкости с 19,53–19,57% до **12,12–12,27%**, давления с 62,49–62,59 до **37,61–37,85 бар**. Это одинаковые старые сценарии для обоих вариантов, не новая независимая проверка. Ошибка ещё велика. [Обучение](z-cold-start-training.json), [протокол](z-cold-start-protocol.json).

Дальнейшая диагностика на старой калибровке показала: 98,1% оставшейся квадратичной ошибки давления приходится на скважины без истории. Их RMSE — 89,04 бар, остальных — 6,66 бар. В закреплённом Google-конфиге отключён вариатный RoPE; до добавления геологии в последнем слое 77 нулевых целевых рядов имеют почти одинаковые представления: максимальное различие 0,000290, после позднего кодирования — 0,735083. [Измерение внутренних представлений](z-identity-diagnosis.json). Это указывает на возможное ограничение позднего кодирования; его причинный вклад и полезность раннего кодирования ещё проверяются.

Коммит `d917906` повторно использует существующий слой геологического кодирования также перед первым блоком Google. Нулевая инициализация сохраняет исходный прогноз, обучение может различать скважины на всех слоях. Проверки тождества до обучения, различимости скважин, восстановления checkpoint и отклонения чужой геологии прошли на A100. Запущены 40 эпох от выбранных весов предыдущего этапа. [Новый протокол](z-early-identity-protocol.json). Изменение выбрано по старым данным разработки; новые тестовые прогнозы ещё не оценивались. Перед независимой оценкой веса будут окончательно закреплены.

Проверка первоисточников через Exa: [Google описывает](https://research.google/blog/timesfm-3-a-zero-shot-foundation-model-for-multivariate-forecasting/) совместные цели, известные будущие ковариаты и маскирование будущих целевых рядов — эти границы сохраняются в нашей реализации. В [исходной реализации RevIN](https://github.com/ts-kim/RevIN/blob/master/RevIN.py) стандартное отклонение включает добавку для численной устойчивости. Нулевой множитель подтверждён именно в установленной закреплённой версии декодера TimesFM, не объявляется свойством всех реализаций RevIN. Выбор обучающего масштаба вместо малой численной добавки — проверяемая адаптация TimesOil, не рекомендация авторов этих источников.

Свежие физические сценарии Z запущены отдельно: лимиты дебита добывающих ×1,13, нагнетательных ×0,91 к опорному графику, затем восемь сочетаний BHP. Все 103 скважины и 224 месяца сохраняются; это новые совместные изменения дебита и BHP. Разбиение 5/3 и запрет выбора весов по новым тестам закреплены [до оценки](z-cold-start-independent-protocol.json). Физические расчёты пока не завершены.

Интервалы в JSON — эмпирическая калибровка на пяти сценариях. Это слишком мало для общего обещания надёжности; в абляции Z одновременное покрытие всей траектории давления нарушается на одном из трёх тестов. Автономный суррогат для произвольных режимов не сертифицирован. Каждое принятое управление по-прежнему проверяется полной физикой и официальной экономикой.

[manifest.json](manifest.json) содержит SHA-256 и исходные пути A100; протоколы запуска приложены. Для проверки финансового пакета: `python3 ../verified_20260910/verify.py`.

## Новая политика Qwen: физическая проверка

Связка обученного TimesFM, эталонной траектории и закреплённой локальной поправки сформировала новую гипотезу: BHP добывающих +7,5 бар, множитель BHP нагнетательных 0,95. Все 103 скважины и 224 месяца проверены OPM и официальным калькулятором. ЧДД 13 510,052491 млн руб., **+13,782080%** к общей базе; кандидат уступил лучшим 14 144,272847 млн руб. и не выбран. Четыре роли подтвердили сохранение лучшего графика. [Полное сравнение](z-local-policy-comparison.json), [квитанция прогноза](z-local-policy-receipt.json).

Первый запрос этого эксперимента повторил базовые управления. В код добавлено отклонение повторного хеша управляющих команд до дорогого прогноза; повтор базы не выдаётся за новое улучшение. Агентам также явно передаются факты загрузки обученных весов и выполнения прогноза, отдельно от незавершённой сертификации точности.
